import { ResultSet } from "https://esm.sh/@libsql/client@0.6.0/web";

interface Props {
  /**
  * @description The description of name.
  */
  retorno?: ResultSet;
}

export default function action(resultado: ResultSet) {

  return resultado
}